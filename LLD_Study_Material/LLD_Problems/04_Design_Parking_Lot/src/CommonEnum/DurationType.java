package CommonEnum;

/**
 * Enum to specify duration type for parking.
 */
public enum DurationType {
    /**
     * Represents a duration in hours.
     */
    HOURS,
    /**
     * Represents a duration in days.
     */
    DAYS
}
