package CommonEnum;

public enum SortOrder {
    TIMESTAMP_DESC,     // Most recent first
    TIMESTAMP_ASC,      // Oldest first
    RELEVANCE          // Future: Can be based on likes, comments, etc.
}
